import 'package:flutter/material.dart';

class FindUserScreen extends StatelessWidget {
  const FindUserScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Find User / Contacts'),
      ),
    );
  }
}